Write a python function for the cellular automata that is called execute. The function should take a #total_functions# parameters where the first #local_functions# parameters represents local function calculations and the remaining are global function calculations. The input parameters should be named: #parameter_names#. Each parameter is an integer array of the size equal to #values#. The function should returns an integer that represnets the tile value (the number of tiles are #values#). The function can use numpy as np or any native python functions.#code_context##current_code##current_fitness#