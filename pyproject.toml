[project]
name = "llm-ca-evolution"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "anthropic>=0.72.0",
    "numpy>=1.24.4",
    "pcg-benchmark",
    "pillow>=10.4.0",
    "python-dotenv>=1.0.1",
    "pyyaml>=6.0.3",
    "tqdm>=4.70.0",
]

[tool.uv.sources]
pcg-benchmark = { git = "https://github.com/amidos2006/pcg_benchmark.git" }
